apiVersion: tdf.7sg.ai/v1
kind: TargetDefinition
metadata:
  name: azure-east
  version: "1.0.0"
  tags:
    provider: azure
target
  provider: azure   # <- missing colon above; deliberately broken YAML
  regions:
    - eastus
