apiVersion: tdf.7sg.ai/v1
kind: TargetDefinition
metadata:
  name: edge-fleet
  version: "1.0.0"
  tags:
    provider: edge
    geo: us
target:
  provider: edge
  regions:
    - edge-zone-1
  services:
    - name: k8s
      type: kubernetes
    - name: store
      type: object-store
    - name: secret-mgr
      type: secrets
